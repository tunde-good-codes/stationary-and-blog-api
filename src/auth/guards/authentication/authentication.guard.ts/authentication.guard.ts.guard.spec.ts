import { AuthenticationGuardTsGuard } from "./authentication.guard";

describe("AuthenticationGuardTsGuard", () => {
  it("should be defined", () => {
    expect(new AuthenticationGuardTsGuard()).toBeDefined();
  });
});
